## Handle failed and pending payments

When using the [`capture` &nearr;](#) endpoint, a [`Transaction` &nearr;](#) is
is returned if there was no error when processing the payment. However, the
payment may still be pending, and a few fail-safes should be put in place.

<hr>

Handle pending transactions through webhooks